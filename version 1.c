#inlcude<stdio.h>
#include <stdio.h>

int main() {
    int a = 11, b = 22, c = 36, x = 4;
    int y;

    y = a * x * x + b * x + c;

    printf("The predicted value is %d units\n", y);

    return 0;
}
